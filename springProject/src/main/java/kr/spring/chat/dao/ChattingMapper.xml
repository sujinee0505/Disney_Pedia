<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.chat.dao.ChattingMapper"> 
	<select id="getChattingDetail" parameterType="map" resultType="ChattingVO"> 
		SELECT 
		 * 
		FROM dchatting c 
		JOIN dmember m 
		ON c.from_num=m.mem_num 
		WHERE chatboard_num=#{chatboard_num} 
		AND ((from_num=#{from_num} AND to_num=#{to_num}) 
		OR (from_num=#{to_num} AND to_num=#{from_num})) 
		ORDER BY send_date ASC
	</select>
</mapper>

