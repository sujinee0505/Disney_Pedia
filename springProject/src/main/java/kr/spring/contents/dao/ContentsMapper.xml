<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.contents.dao.ContentsMapper"> 
	<insert id="insertStar" parameterType="map" >
		INSERT INTO dcontents_star 
			(
				 star_num,
				 star,
				 mem_num,
				 contents_num,
				 contents_type)
		VALUES 
			(
			 <if test="checkComment == 0">
			 	dcontents_star_seq.nextval, 
			 </if>
			 <if test="checkComment == 1">
			 	 #{starVO.star_num}, 
			 </if>
				 #{starVO.star},
				 #{starVO.mem_num}, 
				 #{starVO.contents_num},
				 #{starVO.contents_type})
	</insert>

</mapper>







