<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.comment.dao.CommentMapper"> 
	<select id="selectList" parameterType="map" resultType="CommentVO"> 
		SELECT
		  *
		FROM (SELECT 
		       a.*,
		       rownum rnum
		      FROM (SELECT 
		      		  c.comment_num,
		      		  c.content,
		      		  c.reg_date,
		      		  c.modify_date
		      	   FROM dcomment c
		      	   JOIN dmember m
		      	   ON c.mem_num = m.mem_num	
			  ORDER BY c.comment_num DESC)a)
		<![CDATA[
		WHERE rnum >= #{start} AND rnum <= #{end}
		]]>
	</select>	
	<!-- 내가쓴 코멘트 목록 -->
	<select id="myCommentList" parameterType="map" resultType="CommentVO">
		SELECT
		  *
		FROM (SELECT 
		       a.*,
		       rownum rnum
		      FROM (SELECT 
		      		  *
		      		 FROM
		      		 dcomment WHERE mem_num={mem_num}
		      		 ORDER BY comment_num DESC)a)
		      <![CDATA[
      		 WHERE rnum >= #{start} AND rnum <= #{end}
      		 ]]>  		 
	</select>
</mapper>

